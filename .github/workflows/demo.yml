name: Docker

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Run tests
        run: |
          cd /opt/mellanox/doca/samples/doca_telemetry/telemetry_export/build
          ./doca_telemetry_export
          
